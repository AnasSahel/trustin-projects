{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"],
      "dependsOn": ["db:prisma:push", "db:prisma:generate"]
    },
    "dev": {
      "cache": false,
      "persistent": true
      // "dependsOn": ["db:prisma:push", "db:prisma:generate"]
    }
    // "db:prisma:init": { "cache": false },
    // "db:prisma:push": { "cache": false },
    // "db:prisma:generate": { "cache": false }
  }
}
